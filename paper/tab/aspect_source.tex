% HAND-MAINTAINED -- not generated by code/make_tables.py.
{\setlength{\tabcolsep}{3pt}\footnotesize
\begin{tabular}{@{}l p{0.23\linewidth} l c c c@{}}
\toprule
proposal source & how a proposal is produced & cost per token & trained & conf. & coord. \\
\midrule
small AR model & a separate model, run sequentially & small forward pass & model & $q$ & yes \\
independent heads & $k$ heads on a frozen backbone & one matmul & heads & per pos. & no \\
dependent heads & heads see the drafted prefix & $k$ small passes & heads & per pos. & partly \\
feature-level AR & one decoder layer over hidden states & one layer & layer & $c_j$ & yes \\
self-refinement & the target iterates on its own guesses & target pass & no & -- & via pool \\
prompt retrieval & $n$-gram match against the prompt & string match & no & -- & copied \\
automaton retrieval & suffix automaton over past text & $O(1)$ lookup & no & -- & copied \\
token recycling & top-$k$ adjacency table from past steps & table lookup & no & stale & no \\
independent MTP & one pass, positions predicted apart & one pass & yes & per pos. & no \\
MTP $+$ auxiliaries & one pass, shared $\aux$ (PTP) & one pass & LoRA & $c_k$ & yes \\
\bottomrule
\end{tabular}}
